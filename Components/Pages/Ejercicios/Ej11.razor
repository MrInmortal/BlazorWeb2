@page "/Ej11"
@rendermode InteractiveServer

<h3>Ejercicio 11</h3>
<p>Contruir una funcion que reciba como parametro un entero y un digito y retorne 1 si dicho entero es multiplo de dicho digito y 0 si no es asi </p>

<div class="contenedor">
    <h3>Verificar si un entero es múltiplo de un dígito</h3>
    <p>Construir una función que reciba como parámetro un entero y un dígito, y retorne 1 si dicho entero es múltiplo del dígito, y 0 si no es así.</p>

    <label for="inputNumber" class="form-label">Número entero:</label>
    <input @bind="Ingresar" type="number" class="form-control" id="inputNumber" placeholder="Ingrese un número" />

    <label for="inputDigit" class="form-label">Dígito:</label>
    <input @bind="Digito" type="number" class="form-control" id="inputDigit" placeholder="Ingrese un dígito" min="1" max="9"/>

    <button class="btn-calcular" @onclick="Verificar">Verificar múltiplo</button>

    @if (resultado != null)
    {
        <div class="alerta-info">
            <strong>Resultado:</strong> @resultado
        </div>
    }
</div>

@code {
    private int Ingresar;
    private int Digito;
    private int? resultado;

    private void Verificar()
    {
        if (Digito != 0)
        {
            resultado = Multiplo(Ingresar, Digito) ? 1 : 0;
        }
        else
        {
            resultado = null;
        }
    }

    private bool Multiplo(int numero, int digito)
    {
        return numero % digito == 0;
    }
}
<style>
    body {
        font-family: Arial, sans-serif;
        background: #f4f4f4;
        margin: 0;
        padding: 0;
    }

    .contenedor {
        max-width: 600px;
        margin: 30px auto;
        padding: 20px;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
    }

    h3, p {
        text-align: center;
        color: #333;
    }

    .form-control {
        width: 100%;
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 4px;
        margin-bottom: 20px;
    }

    .btn-calcular {
        background: #007bff;
        color: #fff;
        border: none;
        border-radius: 8px;
        padding: 12px;
        font-size: 18px;
        cursor: pointer;
        width: 100%;
        transition: background 0.3s;
    }

        .btn-calcular:hover {
            background: #0056b3;
        }

    .alerta-info, .alerta-advertencia {
        padding: 15px;
        border-radius: 8px;
        margin-top: 20px;
        font-size: 16px;
    }

    .alerta-info {
        background: #d1ecf1;
        color: #0c5460;
    }

    .alerta-advertencia {
        background: #fff3cd;
        color: #856404;
    }
</style>